let altura_cm = 174;

let altura_mts = 1.74;

let peso_kg = 60.00;

let altura_ceil = Math.ceil(altura_mts);

let altura_floor = Math.floor(peso_kg);

if (Number.MAX_VALUE == Number.MAX_VALUE + 1) {
    console.log(true);
} else {
    console.log(false);
}